fastapi==0.115.0
uvicorn[standard]==0.30.6
python-multipart==0.0.12
pydantic==2.9.2

# Face Detection & Recognition
ultralytics==8.3.0          # YOLOv8
# NOTE: yolov8n-face.pt is NOT auto-downloaded.
#       Manually downloaded from: https://github.com/lindevs/yolov8-face/releases/latest/download/yolov8n-face-lindevs.pt
onnxruntime==1.19.2         # Runtime for ONNX models

# Face Embeddings (ArcFace 512-D)
# Option A (Linux/Windows with MSVC): insightface==0.7.3
# Option B (Windows-compatible, pure Python - INSTALLED):
deepface                    # Wraps TF/Keras ArcFace; auto-downloads weights on first use

# Computer Vision
opencv-python==4.9.0.80
numpy<2  # Pin to NumPy 1.x for compatibility with cv2

# Deep Sort (face tracking)
deep-sort-realtime==1.3.2

# Utilities
aiofiles==24.1.0
pillow==10.4.0
